/*var a=500;

    if(true)
    {
        var a=-200;   //It will override the outer a
        console.log(a)
    }

console.log(a)
*/

/* var b=200;
function demo()
{
    if(true)
    {
        var b=500;      //If same thing we declare in the function then it is behaving different. So we are not using var mostly.
        console.log(b)
    }
}
demo();
console.log(b) */

console.log(a)   //Hoisting is possible in case of var but not in let and const
const a=10;




